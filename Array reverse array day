
#include<stdio.h>
#define PI=3.14f
int main() {
float r;
printf("enter the radius of circle\n");
scanf("%d",&r);
float area=PI*r*r;
printf("area is :%f\n",area);
float per=2*PI*r;
prntf("perameter is:%f",per);
return 0;
}
